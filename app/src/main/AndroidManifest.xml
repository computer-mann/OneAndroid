<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.empublite2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmPubLite2">
        <activity android:name=".Activities.ListViewExample" />
        <activity
            android:name=".Activities.ForTheHorizon"
            android:label="@string/title_activity_layout_for_thehorizon" />
        <activity
            android:name=".Activities.ForRelative"
            android:label="@string/title_activity_for_relative" />
        <activity
            android:name=".Activities.LayoutForLinear"
            android:label="@string/title_activity_layout_for_linear" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>